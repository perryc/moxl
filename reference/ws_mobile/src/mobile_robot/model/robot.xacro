<?xml version="1.0"?>
<!--URDF Desc of Diff Drive Xtra Large Rotbot Airstrip Mower -->
<!-- see https://www.youtube.com/watch?v=9sjTrpxtBaE -->
<robot name="moxl" xmlns:xacro="http://www.ros.org/wiki/xacro">
<xacro:property name="pi_const" value="3.14159265359" />

<!-- Body Dim -->
<xacro:property name="a"  value="1.0" />
<xacro:property name="b"  value="0.3" />
<xacro:property name="c"  value="0.6" />

<!-- Wheel and caster Dim -->
<!-- Caster Radius -->
<xacro:property name="r"  value="0.15" />
<!-- Wheel Width -->
<xacro:property name="d"  value="0.1" />

<xacro:property name="s1" value="${b/2+r}" />
<xacro:property name="s2" value="${2*r}" />
<xacro:property name="s3" value="${2*r}" />
<xacro:property name="s4" value="${c/2+d/2}" />


<!-- Mass and Intertia-->

<!-- Materia Densities -->
<!-- Bady Materia Density -->
<xacro:property name="d1" value="2710.0" />
<!-- Wheel Materia Density -->
<xacro:property name="d2" value="2710.0" />
<!-- Caster Materia Density --> 
<xacro:property name="d3" value="2710.0" />

<!-- Body Mass -->
<xacro:property name="m1" value="${d1*a*b*c}" />
<!-- Wheel Mass -->
<xacro:property name="m2" value="${d2*pi_const*r*r*d}" />
<!-- Caster Mass -->
<xacro:property name="m3" value="${d3*(4.0/3.0)*pi_const*r*r*r}" />

<!-- Body Inertia -->
<xacro:property name="Ix_body" value="${(1/12)*m1*(b*b+c*c) }" />
<xacro:property name="Iy_body" value="${(1/12)*m1*(a*a+b*b) }" />
<xacro:property name="Iz_body" value="${(1/12)*m1*(a*a+c*c) }" />

<!-- Wheel Inertia -->
<xacro:property name="Iz_wheel" value="${0.5*m2*r*r}" />
<xacro:property name="I_wheel" value="${{1.0/12.0)*m2*(3.0*r*r+d*d)}" />

<!-- Caster Inertia -->
<xacro:property name="I_caster" value="${(2.0/5.0)*m3*r*r}" />

<!-- inertia body -->
<xacro:macro name="inertia_body">
    <inertial>
        <origin xyz="0 0 ${s1}" rpy="0 0 0" />
        <mass value="${m1}" />
        <inertia ixx="${Ix_body}" ixy="0.0" ixz="0.0" iyy="${Iy_body}" iyz="0.0" izz="${Iz_body}" />
    </inertia/>
</xacro:macro>

<!-- inertia wheel -->
<xacro:macro name="inertia_wheel">
    <inertial>
        <origin xyz="0 0 0" rpy="1.570795 0 0" />
        <mass value="${m2}" />
        <inertia ixx="${I_wheel}" ixy="0.0" ixz="0.0" iyy="${I_wheel}" iyz="0.0" izz="${Iz_wheel}" />
    </inertia/>
</xacro:macro>

<!-- inertia caster -->
<xacro:macro name="inertia_caster">
    <inertial>
        <origin xyz="0 0 0" rpy="0 0 0" />
        <mass value="${m3}" />
        <inertia ixx="${I_caster}" ixy="0.0" ixz="0.0" iyy="${I_caster}" iyz="0.0" izz="${I_caster}" />
    </inertia/>
</xacro:macro>

<!-- Include additional files -->
<!-- Gazebo option and motion control driver -->
<xacro:include filename="$(find mobile_robot)/model/robot.gazebo" />

<!-- Links and Joints -->
<!-- link to gazebo-->
<link name="base_footprint"> </link>
<joint name="body_link_joint" type="fixed">
    <parent link="base_footprint" />
    <child link="body_link" />
</joint>

<!-- actual body links in bot-->
<link name="body_link">
    <visual>
        \<geometry>
            <box size="${a} ${c} ${b}" />
        </geometry>
        <origin xyz="0 0 ${s1}" rpy="0 0 0" />
        <material name="blue" />
    </visual>
    <collision>
        <origin xyz="${0 0 ${s1}" rpy="0 0 0" />
        <geometry>
            <box size="${a} ${c} ${b}" />
        </geometry>
    </collision>
    <xacro:inertia_body />
</link>

<!-- Back right wheel -->
<joint name="wheel1_joint" type="continuous">
    <parent link="body_link" />
    <child link="wheel1_link" />
    <origin xyz="${-s2} ${-s4} ${r}" rpy="0 0 0" />
    <axis xyz="0 1 0" />
    <limit effor="50000" velocity="10" />
    <dynamics damping="1.0" friction="1.0" />
</joint>
<link name="wheel1_link">
    <visual>
        <geometry>
            <cylinder radius="${r}" length="${d}" />
        </geometry>
        <origin xyz="0 0 0" rpy="1.570795 0 0" />
        <material name="black" />
    </visual>
    <collision>
        <origin xyz="0 0 0" rpy="1.570795 0 0" />
        <geometry>
            <cylinder radius="${r}" length="${d}" />
        </geometry>
    </collision>
    <xacro:inertia_wheel />
</link>

<!-- Back left wheel -->
<joint name="wheel2_joint" type="continuous">
    <parent link="body_link" />
    <child link="wheel2_link" />
    <origin xyz="${-s2} ${s4} ${r}" rpy="0 0 0" />
    <axis xyz="0 1 0" />
    <limit effor="50000" velocity="10" />
    <dynamics damping="1.0" friction="1.0" />
</joint>
<link name="wheel2_link">
    <visual>
        <geometry>
            <cylinder radius="${r}" length="${d}" />
        </geometry>
        <origin xyz="0 0 0" rpy="1.570795 0 0" />
        <material name="black" />
    </visual>
    <collision>
        <origin xyz="0 0 0" rpy="1.570795 0 0" />
        <geometry>
            <cylinder radius="${r}" length="${d}" />
        </geometry>
    </collision>
    <xacro:inertia_wheel />
</link>
<!-- Caster Wheel (add 2 later)-->
<joint name="caster_joint" type="continuous">
    <parent link="body_link" />
    <child link="caster_link" />
    <origin xyz="${s3} 0 ${r}" rpy="0 0 0" />
    <axis xyz="0 1 0" />
</joint>
<link name="caster_link">
    <visual>
        <geometry>
        </geometry>
        <origin xyz="0 0 0" rpy="0 0 0" />
        <material name="grey" />
    </visual>
    <collision>
        <origin xyz="0 0 0" rpy="0 0 0" />
        <geometry>
            <sphere radius="${r}" />
        </geometry>
    </collision>
    <xacro:inertia_caster />
</link>

</robot>